<div class="container">


<div style="text-align: center">
  <h1><%= @user.username %></h1>
  <a href="/following/<%= @user_id %>"><input type="submit" class="btn-danger" value="Following"></a>
  <a href="/followers/<%= @user_id %>"><input type="submit" class="btn-danger" value="Followers"></a>
</div>

  <% @user_tweets.each do |tweet| %>
    <% day = tweet.created_at.day %>
    <% month = tweet.created_at.month %>
    <% year = tweet.created_at.year %>
    <% hour = tweet.created_at.hour %>
    <% minute = tweet.created_at.min %>
    <%= "#{hour}:#{minute} #{month}/#{day}/#{year}" %>
    <%= tweet.content %><br>
  <% end %>
</div>